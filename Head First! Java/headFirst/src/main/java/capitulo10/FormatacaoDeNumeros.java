package capitulo10;

public class FormatacaoDeNumeros {

    /*
        Java tem um método estático de String que permite formatação, o "String.format". Usamos "%" na
        sintaxe do método para informar onde o segundo argumento entrará na string(o valor), usamos a
        letra após a "%" para informar o tipo do retorno(d = decimal, f = float).

     */
}
