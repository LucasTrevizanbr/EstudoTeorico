package capitulo18;

public class RMI {
    /*
        RMI(Remote Method Invocation) permite causar uma chamada em objeto remoto(que esta situado em um heap
        em outro local), mas com um código que lhe permita *simular* que está chamando um método em um
        objeto local.
             Precisamos de auxiliares no lado do cliente e no lado do servidor. Eles manipularão todos os
        detalhes de E/S e rede de nível inferior para que o cliente e o servidor possam agir como se estivessem
        no mesmo heap.
     */
}
