package capitulo3;

public class VisaoGeralVariavelJava {
    /*
        Java é uma linguagem tipada, ou seja, só podemos atribuir valor a nossas variáveis
        que sejam de acordo com o tipo declarado.

        As variáveis em java vem em duas versões: primitivas e de referência de objeto. As
        primitivas contém valores básicos(inteiro, boolean, doubles, float etc) enquanto as
        de referência de objeto SE REFERENCIAM A OBJETOS.

        Variáveis de referência é como nos chegamos ao objeto, quando instânciamos um objeto de
        qualquer tipo, usamos sua variável de referência para acessar os métodos do objeto. A
        responsabilidade de acessar o objeto pelos bytes da variável de referência é puramente da
        jvm. Quando damon um " = new Objeto()" dizemos para jvm alocar espaço na memória heap para
        o objeto.

     */
}
