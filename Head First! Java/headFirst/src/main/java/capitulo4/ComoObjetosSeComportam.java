package capitulo4;

public class ComoObjetosSeComportam {
    /*
        Um objeto tem comportamento e estados, como o comportamento e o estado estão relacionados?
        cada instância de uma classe(objeto) pode ter seus próprios valores para suas variáveis
        de instância.Um objeto tem comportamentos que atuam sobre seus estados, em outras palavras,
        os métodos usam os valores das variáveis de instância para alterarem seu estado.

        Um método pode receber parâmetros, um chamador passa argumentos. Os argumentos são valores
        que você passará para os métodos, esse valor sera inserido diretamente em um parâmetro e um
        parâmetro nada mais é que uma VARIÁVEL LOCAL com tipo e nome que pode ser usada no escopo do
        método. Se um método utiliza um parâmetro VOCÊ TERÁ QUE PASSAR ALGO PARA ELE.

        Métodos podem ter retorno, quando não ha retorno usamos "void", quando há retorno precisamos
        especificar na assinatura do método qual o tipo do retorno E EXPLICITAR O RETORNO.
        Métodos podem receber mais de um parâmetro.

        Nossas classes possuem GETTERS e SETTERS, getters servem para capturarmos o valor do atributo
        com um retorno enquanto setters servem para configurarmos o valor do atributo.




     */
}
